# Condition Research Guide (Issue #487)

목표: 등록된 모든 조건을 나열하고, 코드 기반 파라미터 default와 인터넷 근거(정의/사용 시점/해석/주의점)를 한 문서에 연결합니다.

- 조건 수: **156개**
- 파라미터/기본값 추출 방식: `screener/conditions/*.py`의 `@register_condition` AST 파싱
- 근거는 지표군 단위로 매핑 (`sources` 컬럼 태그 참조)

## Source Index
- **S_RSICORE**: [RSI (정의/해석/파라미터)](https://chartschool.stockcharts.com/table-of-contents/technical-indicators-and-overlays/technical-indicators/relative-strength-index-rsi)
- **S_MACD**: [MACD (정의/신호선 교차)](https://chartschool.stockcharts.com/table-of-contents/technical-indicators-and-overlays/technical-indicators/macd-moving-average-convergence-divergence-oscillator)
- **S_STOCH**: [Stochastic Oscillator](https://chartschool.stockcharts.com/table-of-contents/technical-indicators-and-overlays/technical-indicators/stochastic-oscillator-fast-slow-and-full)
- **S_WILLR**: [Williams %R](https://chartschool.stockcharts.com/table-of-contents/technical-indicators-and-overlays/technical-indicators/williams-r)
- **S_CCI**: [Commodity Channel Index (CCI)](https://chartschool.stockcharts.com/table-of-contents/technical-indicators-and-overlays/technical-indicators/commodity-channel-index-cci)
- **S_MFI**: [Money Flow Index (MFI)](https://chartschool.stockcharts.com/table-of-contents/technical-indicators-and-overlays/technical-indicators/money-flow-index-mfi)
- **S_BB**: [Bollinger BandWidth / %B](https://chartschool.stockcharts.com/table-of-contents/technical-indicators-and-overlays/technical-indicators/bollinger-bandwidth)
- **S_ATR**: [ATR (Average True Range)](https://www.britannica.com/money/average-true-range-indicator)
- **S_ADX**: [ADX / Directional Movement](https://www.britannica.com/money/average-directional-index-adx)
- **S_AROON**: [Aroon / Aroon Oscillator](https://www.investopedia.com/terms/a/aroon.asp)
- **S_ICHI**: [Ichimoku Cloud](https://www.investopedia.com/terms/i/ichimoku-cloud.asp)
- **S_VWAP**: [VWAP](https://www.investopedia.com/terms/v/vwap.asp)
- **S_OBV**: [On-Balance Volume (OBV)](https://chartschool.stockcharts.com/table-of-contents/technical-indicators-and-overlays/technical-indicators/on-balance-volume-obv)
- **S_CHAIKIN**: [Chaikin Money Flow / Oscillator](https://chartschool.stockcharts.com/table-of-contents/technical-indicators-and-overlays/technical-indicators/chaikin-money-flow-cmf)
- **S_DONCHIAN**: [Donchian Channel](https://www.investopedia.com/terms/d/donchianchannels.asp)
- **S_KELTNER**: [Keltner Channel](https://www.investopedia.com/terms/k/keltnerchannel.asp)
- **S_PARABOLIC**: [Parabolic SAR](https://www.investopedia.com/terms/p/parabolicindicator.asp)
- **S_VAL**: [P/E 정의 (규제기관 용어집)](https://www.investor.gov/introduction-investing/investing-basics/glossary/price-earnings-pe-ratio)
- **S_FACTORS**: [Fama/French Data Library (팩터 정의/구성)](https://mba.tuck.dartmouth.edu/pages/faculty/ken.french/data_library.html)
- **S_MOM**: [Momentum Profitability (Jegadeesh & Titman, 1993)](https://www.sciencedirect.com/science/article/abs/pii/0304405X93900235)
- **S_PEAD**: [Post-Earnings-Announcement Drift (Bernard & Thomas, 1989)](https://www.sciencedirect.com/science/article/abs/pii/0304405X89900489)
- **S_ACCRUALS**: [Accrual Anomaly (Sloan, 1996)](https://www.sciencedirect.com/science/article/abs/pii/016541019600010X)
- **S_SEASONAL**: [Day-of-Week Effect 개요](https://en.wikipedia.org/wiki/Day_of_the_week_effect)
- **S_ISSUANCE**: [Share issuance and expected returns (Pontiff & Woodgate, 2008)](https://www.sciencedirect.com/science/article/abs/pii/S0304405X08000064)
- **S_SHORT**: [Short Interest Ratio 개요](https://www.investopedia.com/terms/s/shortinterestratio.asp)
- **S_INSIDER**: [Insider Trading basics (SEC)](https://www.sec.gov/about/reports-publications/investorpubsinsiderhtm)

## Condition Catalog

| key | category | params(default) | 언제 사용하면 좋은가 | 해석 방법 | 주의할 점 | sources |
|---|---|---|---|---|---|---|
| `above_ma` | `movingAverage` | `period`(int)=20, `min_distance_pct`(float)=0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `above_ma`(above ma)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `above_ma` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_distance_pct=0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `above_ma` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `above_ma`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `accruals_ratio` | `quality` | `max_ratio`(float)=0.1 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `accruals_ratio`(accruals ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. Sloan(1996) 발생액 이례현상 검증 맥락에서 자주 사용됩니다. | 예: `max_ratio=0.1`면 해당 지표가 최대 0.1 이하인 종목만 통과합니다. `max_ratio=0.1`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `accruals_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `accruals_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` `S_ACCRUALS` |
| `ad_line_trend` | `moneyflow` | `lookback_days`(int)=20 | "지금 추세가 실제로 이어질 가능성이 높은가?" `ad_line_trend`(ad line trend)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ad_line_trend` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `ad_line_trend` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `ad_line_trend`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_OBV` |
| `adx_trend_strength` | `momentum` | `period`(int)=14, `min_adx`(float)=25.0, `di_direction`(str)=any | "지금 추세가 실제로 이어질 가능성이 높은가?" `adx_trend_strength`(adx trend strength)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `adx_trend_strength` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=14`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_adx=25.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `di_direction=any`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `adx_trend_strength` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `adx_trend_strength`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_ADX` |
| `altman_zscore` | `Fundamental` | `min_zscore`(float)=2.5, `max_zscore`(float)=None | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `altman_zscore`(altman zscore)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `altman_zscore` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_zscore=2.5`면 해당 지표가 최소 2.5 이상인 종목만 통과합니다. `min_zscore=2.5`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_zscore=None`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `altman_zscore` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `altman_zscore`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `analyst_revision_1m` | `fundamental` | `min_revision_pct`(float)=0.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `analyst_revision_1m`(analyst revision 1m)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `analyst_revision_1m` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_revision_pct=0.0`면 해당 지표가 최소 0.0 이상인 종목만 통과합니다. `min_revision_pct=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `analyst_revision_1m` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `analyst_revision_1m`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `analyst_revision_3m` | `fundamental` | `min_revision_pct`(float)=0.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `analyst_revision_3m`(analyst revision 3m)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `analyst_revision_3m` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_revision_pct=0.0`면 해당 지표가 최소 0.0 이상인 종목만 통과합니다. `min_revision_pct=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `analyst_revision_3m` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `analyst_revision_3m`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `aroon_oscillator_signal` | `oscillator` | `period`(int)=25, `min_oscillator`(float)=30.0 | "지금이 과열/과매도 구간이라 되돌림 확률이 높은가?" `aroon_oscillator_signal`(aroon oscillator signal)은(는) 역추세/스윙 타이밍 전략에서 신호 선별에 쓰기 좋습니다. 특히 `aroon_oscillator_signal` 고유 규칙을 다른 osc 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=25`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=25`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_oscillator=30.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `aroon_oscillator_signal` 기준입니다. | 과매수/과매도 신호만으로 역베팅하면 강한 추세에서 손실이 커질 수 있습니다. 추가로 `aroon_oscillator_signal`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_AROON` |
| `aroon_trend_signal` | `momentum` | `period`(int)=25, `min_aroon`(float)=70, `direction`(str)=up | "지금 추세가 실제로 이어질 가능성이 높은가?" `aroon_trend_signal`(aroon trend signal)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `aroon_trend_signal` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=25`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=25`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_aroon=70`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `direction=up`는 전략 방향과 맞춰야 해석이 일관됩니다. 이 해석은 `aroon_trend_signal` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `aroon_trend_signal`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_AROON` |
| `asset_growth_rate` | `quality` | `max_growth_pct`(float)=15.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `asset_growth_rate`(asset growth rate)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `asset_growth_rate` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `max_growth_pct=15.0`면 해당 지표가 최대 15.0 이하인 종목만 통과합니다. `max_growth_pct=15.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `asset_growth_rate` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `asset_growth_rate`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `asset_turnover_ratio` | `fundamental` | `min_ratio`(float)=0.5 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `asset_turnover_ratio`(asset turnover ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `asset_turnover_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_ratio=0.5`면 해당 지표가 최소 0.5 이상인 종목만 통과합니다. `min_ratio=0.5`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `asset_turnover_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `asset_turnover_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `atr_expansion_breakout` | `risk` | `atr_period`(int)=14, `atr_multiplier`(float)=1.2, `breakout_lookback`(int)=20 | "지금 추세가 실제로 이어질 가능성이 높은가?" `atr_expansion_breakout`(atr expansion breakout)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `atr_expansion_breakout` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `atr_period=14`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `atr_period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `atr_multiplier=1.2`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `breakout_lookback=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `atr_expansion_breakout` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `atr_expansion_breakout`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_ATR` |
| `atr_percentile_filter` | `risk` | `atr_period`(int)=14, `lookback_days`(int)=120, `max_percentile`(float)=80.0 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `atr_percentile_filter`(atr percentile filter)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `atr_percentile_filter` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `atr_period=14`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `atr_period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=120`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_percentile=80.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `atr_percentile_filter` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `atr_percentile_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_ATR` |
| `avg_trading_value` | `volume` | `lookback_days`(int)=20, `min_value`(float)=1500000000 | "가격 뒤에서 자금이 실제로 유입되고 있는가?" `avg_trading_value`(avg trading value)은(는) 매집·수급 확인 전략에서 신호 선별에 쓰기 좋습니다. 특히 `avg_trading_value` 고유 규칙을 다른 flow 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_value=1500000000`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `avg_trading_value` 기준입니다. | 저유동성 종목은 체결 왜곡이 커서 지표 신호가 실전 수익으로 연결되지 않을 수 있습니다. 추가로 `avg_trading_value`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `below_ma` | `movingAverage` | `period`(int)=20, `max_distance_pct`(float)=0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `below_ma`(below ma)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `below_ma` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_distance_pct=0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `below_ma` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `below_ma`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `beta_to_benchmark` | `risk` | `lookback_days`(int)=60, `max_beta`(float)=1.2 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `beta_to_benchmark`(beta to benchmark)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `beta_to_benchmark` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_beta=1.2`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `beta_to_benchmark` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `beta_to_benchmark`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `bollinger_percent_b` | `oscillator` | `period`(int)=20, `std_mult`(float)=2.0, `min_percent_b`(float)=0.0, `max_percent_b`(float)=1.0 | "이 조건이 전략 전체의 품질을 올려주는가?" `bollinger_percent_b`(bollinger percent b)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `bollinger_percent_b` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `std_mult=2.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `min_percent_b=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_percent_b=1.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `bollinger_percent_b` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `bollinger_percent_b`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_BB` |
| `bollinger_squeeze_breakout` | `oscillator` | `period`(int)=20, `std_mult`(float)=2.0, `max_width_pct`(float)=10.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `bollinger_squeeze_breakout`(bollinger squeeze breakout)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `bollinger_squeeze_breakout` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `std_mult=2.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `max_width_pct=10.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `bollinger_squeeze_breakout` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `bollinger_squeeze_breakout`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_BB` |
| `bollinger_width` | `accumulation` | `max_width_pct`(float)=15.0, `period`(int)=20, `std_dev`(float)=2.0 | "가격 뒤에서 자금이 실제로 유입되고 있는가?" `bollinger_width`(bollinger width)은(는) 매집·수급 확인 전략에서 신호 선별에 쓰기 좋습니다. 특히 `bollinger_width` 고유 규칙을 다른 flow 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `max_width_pct=15.0`면 해당 지표가 최대 15.0 이하인 종목만 통과합니다. `max_width_pct=15.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다; `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `std_dev=2.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `bollinger_width` 기준입니다. | 저유동성 종목은 체결 왜곡이 커서 지표 신호가 실전 수익으로 연결되지 않을 수 있습니다. 추가로 `bollinger_width`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_BB` |
| `book_to_market_ratio` | `fundamental` | `min_ratio`(float)=0.3 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `book_to_market_ratio`(book to market ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `book_to_market_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_ratio=0.3`면 해당 지표가 최소 0.3 이상인 종목만 통과합니다. `min_ratio=0.3`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `book_to_market_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `book_to_market_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `bottom_breakout` | `breakout` | `lookback_days`(int)=20, `breakout_pct`(float)=5.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `bottom_breakout`(bottom breakout)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `bottom_breakout` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `breakout_pct=5.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `bottom_breakout` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `bottom_breakout`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `breakout_with_volume` | `breakout` | `lookback_days`(int)=20, `breakout_pct`(float)=5.0, `volume_ratio`(float)=1.5, `volume_avg_days`(int)=10, `fresh_only`(bool)=True | "지금 추세가 실제로 이어질 가능성이 높은가?" `breakout_with_volume`(breakout with volume)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `breakout_with_volume` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `breakout_pct=5.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `volume_ratio=1.5`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `volume_avg_days=10`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `fresh_only=True`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `breakout_with_volume` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `breakout_with_volume`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `buyback_yield_filter` | `fundamental` | `min_yield_pct`(float)=1.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `buyback_yield_filter`(buyback yield filter)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `buyback_yield_filter` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_yield_pct=1.0`면 해당 지표가 최소 1.0 이상인 종목만 통과합니다. `min_yield_pct=1.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `buyback_yield_filter` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `buyback_yield_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `calmar_ratio_filter` | `risk` | `lookback_days`(int)=252, `min_calmar`(float)=0.5 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `calmar_ratio_filter`(calmar ratio filter)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `calmar_ratio_filter` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=252`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=252`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_calmar=0.5`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `calmar_ratio_filter` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `calmar_ratio_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `cash_conversion_ratio` | `fundamental` | `min_ratio`(float)=0.8 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `cash_conversion_ratio`(cash conversion ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `cash_conversion_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_ratio=0.8`면 해당 지표가 최소 0.8 이상인 종목만 통과합니다. `min_ratio=0.8`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `cash_conversion_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `cash_conversion_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `cashflow_to_debt_ratio` | `fundamental` | `min_ratio`(float)=0.2 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `cashflow_to_debt_ratio`(cashflow to debt ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `cashflow_to_debt_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_ratio=0.2`면 해당 지표가 최소 0.2 이상인 종목만 통과합니다. `min_ratio=0.2`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `cashflow_to_debt_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `cashflow_to_debt_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `cci_overbought_oversold` | `momentum` | `period`(int)=20, `mode`(str)=oversold, `threshold`(float)=100 | "지금이 과열/과매도 구간이라 되돌림 확률이 높은가?" `cci_overbought_oversold`(cci overbought oversold)은(는) 역추세/스윙 타이밍 전략에서 신호 선별에 쓰기 좋습니다. 특히 `cci_overbought_oversold` 고유 규칙을 다른 osc 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `mode=oversold`는 전략 방향과 맞춰야 해석이 일관됩니다; `threshold=100`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `cci_overbought_oversold` 기준입니다. | 과매수/과매도 신호만으로 역베팅하면 강한 추세에서 손실이 커질 수 있습니다. 추가로 `cci_overbought_oversold`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_CCI` |
| `chaikin_money_flow_signal` | `moneyflow` | `period`(int)=20, `min_cmf`(float)=0.0 | "가격 뒤에서 자금이 실제로 유입되고 있는가?" `chaikin_money_flow_signal`(chaikin money flow signal)은(는) 매집·수급 확인 전략에서 신호 선별에 쓰기 좋습니다. 특히 `chaikin_money_flow_signal` 고유 규칙을 다른 flow 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_cmf=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `chaikin_money_flow_signal` 기준입니다. | 저유동성 종목은 체결 왜곡이 커서 지표 신호가 실전 수익으로 연결되지 않을 수 있습니다. 추가로 `chaikin_money_flow_signal`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_CHAIKIN` |
| `chaikin_oscillator_signal` | `moneyflow` | `fast_period`(int)=3, `slow_period`(int)=10, `min_value`(float)=0.0 | "지금이 과열/과매도 구간이라 되돌림 확률이 높은가?" `chaikin_oscillator_signal`(chaikin oscillator signal)은(는) 역추세/스윙 타이밍 전략에서 신호 선별에 쓰기 좋습니다. 특히 `chaikin_oscillator_signal` 고유 규칙을 다른 osc 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `fast_period=3`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `fast_period=3`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `slow_period=10`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_value=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `chaikin_oscillator_signal` 기준입니다. | 과매수/과매도 신호만으로 역베팅하면 강한 추세에서 손실이 커질 수 있습니다. 추가로 `chaikin_oscillator_signal`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_CHAIKIN` |
| `correlation_to_benchmark` | `risk` | `lookback_days`(int)=60, `max_corr`(float)=0.8 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `correlation_to_benchmark`(correlation to benchmark)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `correlation_to_benchmark` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_corr=0.8`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `correlation_to_benchmark` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `correlation_to_benchmark`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `current_ratio` | `Fundamental` | `min_ratio`(float)=1.0, `max_ratio`(float)=3.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `current_ratio`(current ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `current_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_ratio=1.0`면 해당 지표가 최소 1.0 이상인 종목만 통과합니다. `min_ratio=1.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_ratio=3.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `current_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `current_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `day_of_week_seasonality` | `time` | `target_weekday`(int)=0, `min_avg_return_pct`(float)=0.0 | "특정 시점(실적·캘린더)에 반복되는 패턴이 있는가?" `day_of_week_seasonality`(day of week seasonality)은(는) 이벤트 드리븐/시즌 전략에서 신호 선별에 쓰기 좋습니다. Day-of-the-week effect 같은 캘린더 이례현상을 테스트할 때 유용합니다. | 예: `target_weekday=0`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `target_weekday=0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `min_avg_return_pct=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `day_of_week_seasonality` 기준입니다. | 거래비용·슬리피지 반영 후 알파가 사라지기 쉬워 체결 가능성 검증이 필수입니다. 추가로 `day_of_week_seasonality`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_SEASONAL` |
| `death_cross_50_200` | `movingAverage` | `lookback_days`(int)=5 | "지금 추세가 실제로 이어질 가능성이 높은가?" `death_cross_50_200`(death cross 50 200)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `death_cross_50_200` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=5`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `death_cross_50_200` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `death_cross_50_200`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `debt_service_coverage_ratio` | `fundamental` | `min_ratio`(float)=1.2 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `debt_service_coverage_ratio`(debt service coverage ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `debt_service_coverage_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_ratio=1.2`면 해당 지표가 최소 1.2 이상인 종목만 통과합니다. `min_ratio=1.2`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `debt_service_coverage_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `debt_service_coverage_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `debt_to_equity` | `Fundamental` | `min_de`(float)=None, `max_de`(float)=100.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `debt_to_equity`(debt to equity)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `debt_to_equity` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_de=None`면 해당 지표가 최소 None 이상인 종목만 통과합니다. `min_de=None`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_de=100.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `debt_to_equity` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `debt_to_equity`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `distance_from_200d_high` | `price` | `lookback_days`(int)=200, `max_distance_pct`(float)=15.0 | "이 조건이 전략 전체의 품질을 올려주는가?" `distance_from_200d_high`(distance from 200d high)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `distance_from_200d_high` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=200`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=200`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_distance_pct=15.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `distance_from_200d_high` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `distance_from_200d_high`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `distance_from_52w_high` | `price` | `lookback_days`(int)=252, `max_distance_pct`(float)=10.0 | "이 조건이 전략 전체의 품질을 올려주는가?" `distance_from_52w_high`(distance from 52w high)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `distance_from_52w_high` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=252`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=252`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_distance_pct=10.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `distance_from_52w_high` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `distance_from_52w_high`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_MOM` `S_FACTORS` |
| `distance_from_52w_low` | `price` | `lookback_days`(int)=252, `max_distance_pct`(float)=30.0 | "이 조건이 전략 전체의 품질을 올려주는가?" `distance_from_52w_low`(distance from 52w low)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `distance_from_52w_low` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=252`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=252`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_distance_pct=30.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `distance_from_52w_low` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `distance_from_52w_low`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_MOM` `S_FACTORS` |
| `dividend_growth_5y` | `fundamental` | `min_growth_pct`(float)=3.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `dividend_growth_5y`(dividend growth 5y)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `dividend_growth_5y` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_growth_pct=3.0`면 해당 지표가 최소 3.0 이상인 종목만 통과합니다. `min_growth_pct=3.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `dividend_growth_5y` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `dividend_growth_5y`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `dividend_yield` | `Fundamental` | `min_yield`(float)=3.0, `max_yield`(float)=None | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `dividend_yield`(dividend yield)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `dividend_yield` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_yield=3.0`면 해당 지표가 최소 3.0 이상인 종목만 통과합니다. `min_yield=3.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_yield=None`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `dividend_yield` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `dividend_yield`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `dmi_directional_cross` | `momentum` | `period`(int)=14, `lookback_days`(int)=5 | "지금 추세가 실제로 이어질 가능성이 높은가?" `dmi_directional_cross`(dmi directional cross)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `dmi_directional_cross` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=14`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `dmi_directional_cross` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `dmi_directional_cross`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_ADX` |
| `donchian_channel_breakout` | `breakout` | `lookback_days`(int)=20 | "지금 추세가 실제로 이어질 가능성이 높은가?" `donchian_channel_breakout`(donchian channel breakout)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `donchian_channel_breakout` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `donchian_channel_breakout` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `donchian_channel_breakout`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_DONCHIAN` |
| `downside_volatility_filter` | `risk` | `lookback_days`(int)=60, `max_downside_vol_pct`(float)=20.0 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `downside_volatility_filter`(downside volatility filter)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `downside_volatility_filter` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_downside_vol_pct=20.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `downside_volatility_filter` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `downside_volatility_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `drawdown_from_high` | `price` | `lookback_days`(int)=120, `min_drop_pct`(float)=20.0, `price_field`(str)=high | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `drawdown_from_high`(drawdown from high)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `drawdown_from_high` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=120`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=120`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_drop_pct=20.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `price_field=high`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `drawdown_from_high` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `drawdown_from_high`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `dso_trend_filter` | `Fundamental` | `lookback_years`(int)=1, `min_dso_increase_pct`(float)=5.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `dso_trend_filter`(dso trend filter)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `dso_trend_filter` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_years=1`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_years=1`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_dso_increase_pct=5.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `dso_trend_filter` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `dso_trend_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `earnings_stability_score` | `fundamental` | `lookback_years`(int)=5, `max_cv`(float)=0.35 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `earnings_stability_score`(earnings stability score)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `earnings_stability_score` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_years=5`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_years=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_cv=0.35`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `earnings_stability_score` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `earnings_stability_score`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `earnings_surprise_filter` | `event` | `min_surprise_pct`(float)=0.0 | "특정 시점(실적·캘린더)에 반복되는 패턴이 있는가?" `earnings_surprise_filter`(earnings surprise filter)은(는) 이벤트 드리븐/시즌 전략에서 신호 선별에 쓰기 좋습니다. 특히 `earnings_surprise_filter` 고유 규칙을 다른 event 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_surprise_pct=0.0`면 해당 지표가 최소 0.0 이상인 종목만 통과합니다. `min_surprise_pct=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `earnings_surprise_filter` 기준입니다. | 거래비용·슬리피지 반영 후 알파가 사라지기 쉬워 체결 가능성 검증이 필수입니다. 추가로 `earnings_surprise_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_PEAD` |
| `earnings_yield` | `Fundamental` | `min_yield`(float)=8.0, `max_yield`(float)=None | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `earnings_yield`(earnings yield)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `earnings_yield` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_yield=8.0`면 해당 지표가 최소 8.0 이상인 종목만 통과합니다. `min_yield=8.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_yield=None`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `earnings_yield` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `earnings_yield`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `ebit_ev` | `Fundamental` | `min_ebit_ev`(float)=10.0, `max_ebit_ev`(float)=None | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `ebit_ev`(ebit ev)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 그린블라트의 마법공식(Earnings Yield) 관점과 연결됩니다. | 예: `min_ebit_ev=10.0`면 해당 지표가 최소 10.0 이상인 종목만 통과합니다. `min_ebit_ev=10.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_ebit_ev=None`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `ebit_ev` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `ebit_ev`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `ema_cross` | `momentum` | `fast_period`(int)=12, `slow_period`(int)=26, `lookback_days`(int)=5, `direction`(str)=bullish | "지금 추세가 실제로 이어질 가능성이 높은가?" `ema_cross`(ema cross)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ema_cross` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `fast_period=12`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `fast_period=12`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `slow_period=26`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `direction=bullish`는 전략 방향과 맞춰야 해석이 일관됩니다. 이 해석은 `ema_cross` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `ema_cross`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `ema_slope` | `momentum` | `period`(int)=20, `lookback_days`(int)=5, `min_slope_pct`(float)=0.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `ema_slope`(ema slope)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ema_slope` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_slope_pct=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `ema_slope` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `ema_slope`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `eps_cagr_3y` | `fundamental` | `min_cagr_pct`(float)=5.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `eps_cagr_3y`(eps cagr 3y)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `eps_cagr_3y` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_cagr_pct=5.0`면 해당 지표가 최소 5.0 이상인 종목만 통과합니다. `min_cagr_pct=5.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `eps_cagr_3y` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `eps_cagr_3y`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `eps_growth_yoy` | `fundamental` | `min_growth_pct`(float)=5.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `eps_growth_yoy`(eps growth yoy)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `eps_growth_yoy` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_growth_pct=5.0`면 해당 지표가 최소 5.0 이상인 종목만 통과합니다. `min_growth_pct=5.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `eps_growth_yoy` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `eps_growth_yoy`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `ev_to_ebitda_ratio` | `fundamental` | `max_ratio`(float)=12.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `ev_to_ebitda_ratio`(ev to ebitda ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ev_to_ebitda_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `max_ratio=12.0`면 해당 지표가 최대 12.0 이하인 종목만 통과합니다. `max_ratio=12.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `ev_to_ebitda_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `ev_to_ebitda_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `ev_to_sales_ratio` | `fundamental` | `max_ratio`(float)=4.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `ev_to_sales_ratio`(ev to sales ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ev_to_sales_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `max_ratio=4.0`면 해당 지표가 최대 4.0 이하인 종목만 통과합니다. `max_ratio=4.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `ev_to_sales_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `ev_to_sales_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `fcf_yield` | `Fundamental` | `min_fcf_yield`(float)=5.0, `max_fcf_yield`(float)=None | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `fcf_yield`(fcf yield)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `fcf_yield` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_fcf_yield=5.0`면 해당 지표가 최소 5.0 이상인 종목만 통과합니다. `min_fcf_yield=5.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_fcf_yield=None`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `fcf_yield` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `fcf_yield`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `free_cash_flow_margin` | `fundamental` | `min_margin_pct`(float)=5.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `free_cash_flow_margin`(free cash flow margin)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `free_cash_flow_margin` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_margin_pct=5.0`면 해당 지표가 최소 5.0 이상인 종목만 통과합니다. `min_margin_pct=5.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `free_cash_flow_margin` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `free_cash_flow_margin`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `fresh_breakout` | `breakout` | `lookback_days`(int)=20, `breakout_pct`(float)=5.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `fresh_breakout`(fresh breakout)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `fresh_breakout` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `breakout_pct=5.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `fresh_breakout` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `fresh_breakout`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `gap_down_exhaustion` | `price` | `min_gap_down_pct`(float)=2.0 | "특정 시점(실적·캘린더)에 반복되는 패턴이 있는가?" `gap_down_exhaustion`(gap down exhaustion)은(는) 이벤트 드리븐/시즌 전략에서 신호 선별에 쓰기 좋습니다. 특히 `gap_down_exhaustion` 고유 규칙을 다른 event 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_gap_down_pct=2.0`면 해당 지표가 최소 2.0 이상인 종목만 통과합니다. `min_gap_down_pct=2.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `gap_down_exhaustion` 기준입니다. | 거래비용·슬리피지 반영 후 알파가 사라지기 쉬워 체결 가능성 검증이 필수입니다. 추가로 `gap_down_exhaustion`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `gap_up_breakaway` | `price` | `min_gap_pct`(float)=1.0 | "특정 시점(실적·캘린더)에 반복되는 패턴이 있는가?" `gap_up_breakaway`(gap up breakaway)은(는) 이벤트 드리븐/시즌 전략에서 신호 선별에 쓰기 좋습니다. 특히 `gap_up_breakaway` 고유 규칙을 다른 event 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_gap_pct=1.0`면 해당 지표가 최소 1.0 이상인 종목만 통과합니다. `min_gap_pct=1.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `gap_up_breakaway` 기준입니다. | 거래비용·슬리피지 반영 후 알파가 사라지기 쉬워 체결 가능성 검증이 필수입니다. 추가로 `gap_up_breakaway`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `golden_cross_50_200` | `movingAverage` | `lookback_days`(int)=5 | "지금 추세가 실제로 이어질 가능성이 높은가?" `golden_cross_50_200`(golden cross 50 200)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `golden_cross_50_200` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=5`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `golden_cross_50_200` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `golden_cross_50_200`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `gross_margin` | `fundamental` | `min_margin_pct`(float)=20.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `gross_margin`(gross margin)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `gross_margin` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_margin_pct=20.0`면 해당 지표가 최소 20.0 이상인 종목만 통과합니다. `min_margin_pct=20.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `gross_margin` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `gross_margin`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `gross_profitability` | `quality` | `min_ratio`(float)=0.2 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `gross_profitability`(gross profitability)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `gross_profitability` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_ratio=0.2`면 해당 지표가 최소 0.2 이상인 종목만 통과합니다. `min_ratio=0.2`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `gross_profitability` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `gross_profitability`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `ichimoku_cloud_breakout` | `momentum` | `conversion_period`(int)=9, `base_period`(int)=26, `span_b_period`(int)=52 | "지금 추세가 실제로 이어질 가능성이 높은가?" `ichimoku_cloud_breakout`(ichimoku cloud breakout)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ichimoku_cloud_breakout` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `conversion_period=9`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `conversion_period=9`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `base_period=26`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `span_b_period=52`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `ichimoku_cloud_breakout` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `ichimoku_cloud_breakout`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_ICHI` |
| `ichimoku_tenkan_kijun_cross` | `momentum` | `conversion_period`(int)=9, `base_period`(int)=26, `lookback_days`(int)=5 | "지금 추세가 실제로 이어질 가능성이 높은가?" `ichimoku_tenkan_kijun_cross`(ichimoku tenkan kijun cross)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ichimoku_tenkan_kijun_cross` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `conversion_period=9`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `conversion_period=9`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `base_period=26`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `ichimoku_tenkan_kijun_cross` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `ichimoku_tenkan_kijun_cross`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_ICHI` |
| `insider_net_buying` | `fundamental` | `min_net_buying_pct`(float)=0.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `insider_net_buying`(insider net buying)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `insider_net_buying` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_net_buying_pct=0.0`면 해당 지표가 최소 0.0 이상인 종목만 통과합니다. `min_net_buying_pct=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `insider_net_buying` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `insider_net_buying`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` `S_INSIDER` |
| `interest_coverage_ratio` | `fundamental` | `min_ratio`(float)=3.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `interest_coverage_ratio`(interest coverage ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `interest_coverage_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_ratio=3.0`면 해당 지표가 최소 3.0 이상인 종목만 통과합니다. `min_ratio=3.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `interest_coverage_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `interest_coverage_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `intraday_return_filter` | `risk` | `min_intraday_return_pct`(float)=0.0 | "특정 시점(실적·캘린더)에 반복되는 패턴이 있는가?" `intraday_return_filter`(intraday return filter)은(는) 이벤트 드리븐/시즌 전략에서 신호 선별에 쓰기 좋습니다. 특히 `intraday_return_filter` 고유 규칙을 다른 event 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_intraday_return_pct=0.0`면 해당 지표가 최소 0.0 이상인 종목만 통과합니다. `min_intraday_return_pct=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `intraday_return_filter` 기준입니다. | 거래비용·슬리피지 반영 후 알파가 사라지기 쉬워 체결 가능성 검증이 필수입니다. 추가로 `intraday_return_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `inventory_turnover_ratio` | `fundamental` | `min_ratio`(float)=3.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `inventory_turnover_ratio`(inventory turnover ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `inventory_turnover_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_ratio=3.0`면 해당 지표가 최소 3.0 이상인 종목만 통과합니다. `min_ratio=3.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `inventory_turnover_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `inventory_turnover_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `keltner_channel_breakout` | `breakout` | `ema_period`(int)=20, `atr_period`(int)=20, `atr_multiplier`(float)=2.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `keltner_channel_breakout`(keltner channel breakout)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `keltner_channel_breakout` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `ema_period=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `ema_period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `atr_period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `atr_multiplier=2.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `keltner_channel_breakout` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `keltner_channel_breakout`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_KELTNER` |
| `linear_regression_angle_filter` | `momentum` | `lookback_days`(int)=60, `min_angle_deg`(float)=0.0 | "이 조건이 전략 전체의 품질을 올려주는가?" `linear_regression_angle_filter`(linear regression angle filter)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `linear_regression_angle_filter` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_angle_deg=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `linear_regression_angle_filter` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `linear_regression_angle_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `linear_regression_r2_filter` | `momentum` | `lookback_days`(int)=60, `min_r2`(float)=0.5 | "이 조건이 전략 전체의 품질을 올려주는가?" `linear_regression_r2_filter`(linear regression r2 filter)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `linear_regression_r2_filter` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_r2=0.5`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `linear_regression_r2_filter` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `linear_regression_r2_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `linear_regression_slope_filter` | `momentum` | `lookback_days`(int)=60, `min_slope`(float)=0.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `linear_regression_slope_filter`(linear regression slope filter)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `linear_regression_slope_filter` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_slope=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `linear_regression_slope_filter` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `linear_regression_slope_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `ma_cross_down` | `movingAverage` | `short_period`(int)=20, `long_period`(int)=60, `lookback_days`(int)=5 | "지금 추세가 실제로 이어질 가능성이 높은가?" `ma_cross_down`(ma cross down)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ma_cross_down` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `short_period=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `short_period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `long_period=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `ma_cross_down` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `ma_cross_down`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `ma_cross_up` | `movingAverage` | `short_period`(int)=5, `long_period`(int)=20, `lookback_days`(int)=5 | "지금 추세가 실제로 이어질 가능성이 높은가?" `ma_cross_up`(ma cross up)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ma_cross_up` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `short_period=5`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `short_period=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `long_period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `ma_cross_up` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `ma_cross_up`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `ma_ribbon_alignment` | `movingAverage` | `short_period`(int)=20, `mid_period`(int)=50, `long_period`(int)=200 | "지금 추세가 실제로 이어질 가능성이 높은가?" `ma_ribbon_alignment`(ma ribbon alignment)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ma_ribbon_alignment` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `short_period=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `short_period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `mid_period=50`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `long_period=200`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `ma_ribbon_alignment` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `ma_ribbon_alignment`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `ma_touch` | `movingAverage` | `period`(int)=120, `threshold`(float)=0.02 | "지금 추세가 실제로 이어질 가능성이 높은가?" `ma_touch`(ma touch)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ma_touch` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=120`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=120`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `threshold=0.02`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `ma_touch` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `ma_touch`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `macd_histogram_slope` | `momentum` | `fast_period`(int)=12, `slow_period`(int)=26, `signal_period`(int)=9, `lookback_days`(int)=3, `min_slope`(float)=0.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `macd_histogram_slope`(macd histogram slope)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `macd_histogram_slope` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `fast_period=12`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `fast_period=12`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `slow_period=26`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `signal_period=9`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=3`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_slope=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `macd_histogram_slope` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `macd_histogram_slope`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_MACD` |
| `macd_signal_cross` | `momentum` | `fast_period`(int)=12, `slow_period`(int)=26, `signal_period`(int)=9, `lookback_days`(int)=5, `direction`(str)=bullish | "지금 추세가 실제로 이어질 가능성이 높은가?" `macd_signal_cross`(macd signal cross)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `macd_signal_cross` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `fast_period=12`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `fast_period=12`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `slow_period=26`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `signal_period=9`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `direction=bullish`는 전략 방향과 맞춰야 해석이 일관됩니다. 이 해석은 `macd_signal_cross` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `macd_signal_cross`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_MACD` |
| `max_drawdown_window_filter` | `risk` | `lookback_days`(int)=120, `max_drawdown_pct`(float)=20.0 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `max_drawdown_window_filter`(max drawdown window filter)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `max_drawdown_window_filter` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=120`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=120`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_drawdown_pct=20.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `max_drawdown_window_filter` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `max_drawdown_window_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `max_price` | `price` | `max_price`(float)=100000 | "이 조건이 전략 전체의 품질을 올려주는가?" `max_price`(max price)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `max_price` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `max_price=100000`면 해당 지표가 최대 100000 이하인 종목만 통과합니다. `max_price=100000`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `max_price` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `max_price`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `min_price` | `price` | `min_price`(float)=5000 | "이 조건이 전략 전체의 품질을 올려주는가?" `min_price`(min price)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `min_price` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_price=5000`면 해당 지표가 최소 5000 이상인 종목만 통과합니다. `min_price=5000`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `min_price` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `min_price`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `min_volume` | `volume` | `min_volume`(int)=100000 | "가격 뒤에서 자금이 실제로 유입되고 있는가?" `min_volume`(min volume)은(는) 매집·수급 확인 전략에서 신호 선별에 쓰기 좋습니다. 특히 `min_volume` 고유 규칙을 다른 flow 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_volume=100000`면 해당 지표가 최소 100000 이상인 종목만 통과합니다. `min_volume=100000`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `min_volume` 기준입니다. | 저유동성 종목은 체결 왜곡이 커서 지표 신호가 실전 수익으로 연결되지 않을 수 있습니다. 추가로 `min_volume`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `momentum_12_1` | `momentum` | `lookback_months`(int)=12, `skip_recent_months`(int)=1, `min_return_pct`(float)=20.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `momentum_12_1`(momentum 12 1)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. Jegadeesh & Titman(1993) 모멘텀 팩터의 실전형 구현에 가깝습니다. | 예: `lookback_months=12`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_months=12`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `skip_recent_months=1`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `min_return_pct=20.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `momentum_12_1` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `momentum_12_1`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_MOM` `S_FACTORS` |
| `money_flow_index_signal` | `momentum` | `period`(int)=14, `mode`(str)=oversold, `oversold`(float)=20, `overbought`(float)=80 | "이 조건이 전략 전체의 품질을 올려주는가?" `money_flow_index_signal`(money flow index signal)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `money_flow_index_signal` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=14`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `mode=oversold`는 전략 방향과 맞춰야 해석이 일관됩니다; `oversold=20`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `overbought=80`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `money_flow_index_signal` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `money_flow_index_signal`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_MFI` |
| `month_of_year_seasonality` | `time` | `target_month`(int)=1, `min_avg_return_pct`(float)=0.0 | "특정 시점(실적·캘린더)에 반복되는 패턴이 있는가?" `month_of_year_seasonality`(month of year seasonality)은(는) 이벤트 드리븐/시즌 전략에서 신호 선별에 쓰기 좋습니다. 월 효과(Turn-of-the-year 포함) 검증에 자주 사용됩니다. | 예: `target_month=1`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `target_month=1`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `min_avg_return_pct=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `month_of_year_seasonality` 기준입니다. | 거래비용·슬리피지 반영 후 알파가 사라지기 쉬워 체결 가능성 검증이 필수입니다. 추가로 `month_of_year_seasonality`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_SEASONAL` |
| `natr_filter` | `risk` | `period`(int)=14, `max_natr_pct`(float)=5.0 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `natr_filter`(natr filter)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `natr_filter` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=14`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_natr_pct=5.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `natr_filter` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `natr_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_ATR` |
| `net_debt_to_ebitda` | `fundamental` | `max_ratio`(float)=3.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `net_debt_to_ebitda`(net debt to ebitda)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `net_debt_to_ebitda` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `max_ratio=3.0`면 해당 지표가 최대 3.0 이하인 종목만 통과합니다. `max_ratio=3.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `net_debt_to_ebitda` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `net_debt_to_ebitda`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `net_margin` | `fundamental` | `min_margin_pct`(float)=5.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `net_margin`(net margin)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `net_margin` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_margin_pct=5.0`면 해당 지표가 최소 5.0 이상인 종목만 통과합니다. `min_margin_pct=5.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `net_margin` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `net_margin`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `net_share_issuance_filter` | `fundamental` | `max_issuance_pct`(float)=1.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `net_share_issuance_filter`(net share issuance filter)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `net_share_issuance_filter` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `max_issuance_pct=1.0`면 해당 지표가 최대 1.0 이하인 종목만 통과합니다. `max_issuance_pct=1.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `net_share_issuance_filter` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `net_share_issuance_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` `S_ISSUANCE` |
| `obv_divergence` | `accumulation` | `price_max_range_pct`(float)=5.0, `obv_min_change_pct`(float)=5.0, `period`(int)=20 | "가격 뒤에서 자금이 실제로 유입되고 있는가?" `obv_divergence`(obv divergence)은(는) 매집·수급 확인 전략에서 신호 선별에 쓰기 좋습니다. 특히 `obv_divergence` 고유 규칙을 다른 flow 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `price_max_range_pct=5.0`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `price_max_range_pct=5.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `obv_min_change_pct=5.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `obv_divergence` 기준입니다. | 저유동성 종목은 체결 왜곡이 커서 지표 신호가 실전 수익으로 연결되지 않을 수 있습니다. 추가로 `obv_divergence`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_OBV` |
| `obv_trend` | `accumulation` | `direction`(str)=up, `lookback`(int)=20 | "지금 추세가 실제로 이어질 가능성이 높은가?" `obv_trend`(obv trend)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `obv_trend` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `direction=up`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `direction=up`는 전략 방향과 맞춰야 해석이 일관됩니다; `lookback=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `obv_trend` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `obv_trend`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_OBV` |
| `opening_range_breakout` | `price` | `direction`(str)=up | "지금 추세가 실제로 이어질 가능성이 높은가?" `opening_range_breakout`(opening range breakout)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `opening_range_breakout` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `direction=up`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `direction=up`는 전략 방향과 맞춰야 해석이 일관됩니다. 이 해석은 `opening_range_breakout` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `opening_range_breakout`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `operating_margin` | `fundamental` | `min_margin_pct`(float)=10.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `operating_margin`(operating margin)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `operating_margin` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_margin_pct=10.0`면 해당 지표가 최소 10.0 이상인 종목만 통과합니다. `min_margin_pct=10.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `operating_margin` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `operating_margin`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `overnight_return_filter` | `time` | `min_overnight_return_pct`(float)=0.0 | "특정 시점(실적·캘린더)에 반복되는 패턴이 있는가?" `overnight_return_filter`(overnight return filter)은(는) 이벤트 드리븐/시즌 전략에서 신호 선별에 쓰기 좋습니다. 특히 `overnight_return_filter` 고유 규칙을 다른 event 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_overnight_return_pct=0.0`면 해당 지표가 최소 0.0 이상인 종목만 통과합니다. `min_overnight_return_pct=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `overnight_return_filter` 기준입니다. | 거래비용·슬리피지 반영 후 알파가 사라지기 쉬워 체결 가능성 검증이 필수입니다. 추가로 `overnight_return_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `parabolic_sar_flip` | `momentum` | `lookback_days`(int)=5, `af_step`(float)=0.02, `af_max`(float)=0.2 | "이 조건이 전략 전체의 품질을 올려주는가?" `parabolic_sar_flip`(parabolic sar flip)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `parabolic_sar_flip` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=5`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `af_step=0.02`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `af_max=0.2`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `parabolic_sar_flip` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `parabolic_sar_flip`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_PARABOLIC` |
| `payout_ratio_filter` | `fundamental` | `max_payout_pct`(float)=70.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `payout_ratio_filter`(payout ratio filter)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `payout_ratio_filter` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `max_payout_pct=70.0`면 해당 지표가 최대 70.0 이하인 종목만 통과합니다. `max_payout_pct=70.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `payout_ratio_filter` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `payout_ratio_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `pb_ratio` | `Fundamental` | `min_pb`(float)=None, `max_pb`(float)=1.5, `exclude_negative_bv`(bool)=True | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `pb_ratio`(pb ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `pb_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_pb=None`면 해당 지표가 최소 None 이상인 종목만 통과합니다. `min_pb=None`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_pb=1.5`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다; `exclude_negative_bv=True`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `pb_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `pb_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `pcf_ratio` | `Fundamental` | `min_pcf`(float)=None, `max_pcf`(float)=10.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `pcf_ratio`(pcf ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `pcf_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_pcf=None`면 해당 지표가 최소 None 이상인 종목만 통과합니다. `min_pcf=None`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_pcf=10.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `pcf_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `pcf_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `pe_ratio` | `Fundamental` | `min_pe`(float)=None, `max_pe`(float)=15.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `pe_ratio`(pe ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 조엘 그린블라트식 밸류 필터와 결합하기 좋습니다. | 예: `min_pe=None`면 해당 지표가 최소 None 이상인 종목만 통과합니다. `min_pe=None`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_pe=15.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `pe_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `pe_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `peg_ratio` | `Fundamental` | `min_peg`(float)=None, `max_peg`(float)=1.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `peg_ratio`(peg ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `peg_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_peg=None`면 해당 지표가 최소 None 이상인 종목만 통과합니다. `min_peg=None`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_peg=1.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `peg_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `peg_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `piotroski_fscore` | `Fundamental` | `min_score`(int)=7, `max_score`(int)=None | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `piotroski_fscore`(piotroski fscore)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `piotroski_fscore` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_score=7`면 해당 지표가 최소 7 이상인 종목만 통과합니다. `min_score=7`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_score=None`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `piotroski_fscore` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `piotroski_fscore`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `pivot_point_breakout` | `price` | `direction`(str)=up | "지금 추세가 실제로 이어질 가능성이 높은가?" `pivot_point_breakout`(pivot point breakout)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `pivot_point_breakout` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `direction=up`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `direction=up`는 전략 방향과 맞춰야 해석이 일관됩니다. 이 해석은 `pivot_point_breakout` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `pivot_point_breakout`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `post_earnings_drift` | `event` | `min_return_pct`(float)=0.0 | "특정 시점(실적·캘린더)에 반복되는 패턴이 있는가?" `post_earnings_drift`(post earnings drift)은(는) 이벤트 드리븐/시즌 전략에서 신호 선별에 쓰기 좋습니다. Bernard & Thomas(1989) PEAD 맥락에서 많이 쓰입니다. | 예: `min_return_pct=0.0`면 해당 지표가 최소 0.0 이상인 종목만 통과합니다. `min_return_pct=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `post_earnings_drift` 기준입니다. | 거래비용·슬리피지 반영 후 알파가 사라지기 쉬워 체결 가능성 검증이 필수입니다. 추가로 `post_earnings_drift`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_PEAD` |
| `ppo_signal_cross` | `oscillator` | `fast_period`(int)=12, `slow_period`(int)=26, `signal_period`(int)=9, `lookback_days`(int)=5 | "지금 추세가 실제로 이어질 가능성이 높은가?" `ppo_signal_cross`(ppo signal cross)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ppo_signal_cross` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `fast_period=12`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `fast_period=12`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `slow_period=26`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `signal_period=9`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `ppo_signal_cross` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `ppo_signal_cross`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_MACD` |
| `pre_earnings_drift` | `event` | `min_return_pct`(float)=0.0 | "특정 시점(실적·캘린더)에 반복되는 패턴이 있는가?" `pre_earnings_drift`(pre earnings drift)은(는) 이벤트 드리븐/시즌 전략에서 신호 선별에 쓰기 좋습니다. 실적 발표 전 기대감 프라이싱을 다루는 이벤트 전략에 적합합니다. | 예: `min_return_pct=0.0`면 해당 지표가 최소 0.0 이상인 종목만 통과합니다. `min_return_pct=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `pre_earnings_drift` 기준입니다. | 거래비용·슬리피지 반영 후 알파가 사라지기 쉬워 체결 가능성 검증이 필수입니다. 추가로 `pre_earnings_drift`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_PEAD` |
| `price_change` | `price` | `min_change_pct`(float)=None, `max_change_pct`(float)=None, `days`(int)=1 | "이 조건이 전략 전체의 품질을 올려주는가?" `price_change`(price change)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `price_change` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_change_pct=None`면 해당 지표가 최소 None 이상인 종목만 통과합니다. `min_change_pct=None`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_change_pct=None`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다; `days=1`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `price_change` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `price_change`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `price_flat` | `accumulation` | `max_range_pct`(float)=10.0, `period`(int)=20 | "가격 뒤에서 자금이 실제로 유입되고 있는가?" `price_flat`(price flat)은(는) 매집·수급 확인 전략에서 신호 선별에 쓰기 좋습니다. 특히 `price_flat` 고유 규칙을 다른 flow 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `max_range_pct=10.0`면 해당 지표가 최대 10.0 이하인 종목만 통과합니다. `max_range_pct=10.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다; `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `price_flat` 기준입니다. | 저유동성 종목은 체결 왜곡이 커서 지표 신호가 실전 수익으로 연결되지 않을 수 있습니다. 추가로 `price_flat`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `price_range` | `price` | `min_price`(float)=0, `max_price`(float)=999999 | "이 조건이 전략 전체의 품질을 올려주는가?" `price_range`(price range)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `price_range` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_price=0`면 해당 지표가 최소 0 이상인 종목만 통과합니다. `min_price=0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_price=999999`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `price_range` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `price_range`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `price_to_tangible_book` | `fundamental` | `max_ratio`(float)=3.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `price_to_tangible_book`(price to tangible book)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `price_to_tangible_book` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `max_ratio=3.0`면 해당 지표가 최대 3.0 이하인 종목만 통과합니다. `max_ratio=3.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `price_to_tangible_book` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `price_to_tangible_book`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `ps_ratio` | `Fundamental` | `min_ps`(float)=None, `max_ps`(float)=2.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `ps_ratio`(ps ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ps_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_ps=None`면 해당 지표가 최소 None 이상인 종목만 통과합니다. `min_ps=None`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_ps=2.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `ps_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `ps_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `quality_score` | `quality` | `min_score`(float)=0.5 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `quality_score`(quality score)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `quality_score` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_score=0.5`면 해당 지표가 최소 0.5 이상인 종목만 통과합니다. `min_score=0.5`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `quality_score` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `quality_score`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `receivables_turnover_ratio` | `fundamental` | `min_ratio`(float)=4.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `receivables_turnover_ratio`(receivables turnover ratio)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `receivables_turnover_ratio` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_ratio=4.0`면 해당 지표가 최소 4.0 이상인 종목만 통과합니다. `min_ratio=4.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `receivables_turnover_ratio` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `receivables_turnover_ratio`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `relative_strength_vs_benchmark` | `momentum` | `lookback_days`(int)=63, `min_excess_return_pct`(float)=5.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `relative_strength_vs_benchmark`(relative strength vs benchmark)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `relative_strength_vs_benchmark` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=63`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=63`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_excess_return_pct=5.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `relative_strength_vs_benchmark` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `relative_strength_vs_benchmark`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_MOM` `S_FACTORS` |
| `relative_volume_percentile` | `volume` | `lookback_days`(int)=60, `min_percentile`(float)=70.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `relative_volume_percentile`(relative volume percentile)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `relative_volume_percentile` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_percentile=70.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `relative_volume_percentile` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `relative_volume_percentile`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `resistance_breakout` | `breakout` | `lookback_days`(int)=20, `breakout_margin_pct`(float)=0.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `resistance_breakout`(resistance breakout)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `resistance_breakout` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `breakout_margin_pct=0.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `resistance_breakout` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `resistance_breakout`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `resistance_retest_signal` | `price_action` | `resistance_lookback`(int)=40, `tolerance_pct`(float)=1.0 | "이 조건이 전략 전체의 품질을 올려주는가?" `resistance_retest_signal`(resistance retest signal)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `resistance_retest_signal` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `resistance_lookback=40`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `resistance_lookback=40`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `tolerance_pct=1.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `resistance_retest_signal` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `resistance_retest_signal`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `return_kurtosis_filter` | `risk` | `lookback_days`(int)=60, `max_kurtosis`(float)=10.0 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `return_kurtosis_filter`(return kurtosis filter)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `return_kurtosis_filter` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_kurtosis=10.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `return_kurtosis_filter` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `return_kurtosis_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `return_skewness_filter` | `risk` | `lookback_days`(int)=60, `min_skewness`(float)=-1.0 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `return_skewness_filter`(return skewness filter)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `return_skewness_filter` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_skewness=-1.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `return_skewness_filter` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `return_skewness_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `return_turnaround` | `price` | `period_days`(int)=5, `prev_max_return_pct`(float)=-2.0, `min_return_pct`(float)=2.0 | "이 조건이 전략 전체의 품질을 올려주는가?" `return_turnaround`(return turnaround)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `return_turnaround` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period_days=5`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `prev_max_return_pct=-2.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `min_return_pct=2.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `return_turnaround` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `return_turnaround`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `revenue_cagr_3y` | `fundamental` | `min_cagr_pct`(float)=5.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `revenue_cagr_3y`(revenue cagr 3y)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `revenue_cagr_3y` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_cagr_pct=5.0`면 해당 지표가 최소 5.0 이상인 종목만 통과합니다. `min_cagr_pct=5.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `revenue_cagr_3y` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `revenue_cagr_3y`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `revenue_growth_yoy` | `fundamental` | `min_growth_pct`(float)=5.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `revenue_growth_yoy`(revenue growth yoy)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `revenue_growth_yoy` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_growth_pct=5.0`면 해당 지표가 최소 5.0 이상인 종목만 통과합니다. `min_growth_pct=5.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `revenue_growth_yoy` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `revenue_growth_yoy`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `revenue_stability_score` | `fundamental` | `lookback_years`(int)=5, `max_cv`(float)=0.25 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `revenue_stability_score`(revenue stability score)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `revenue_stability_score` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_years=5`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_years=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_cv=0.25`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `revenue_stability_score` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `revenue_stability_score`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `roa_filter` | `fundamental` | `min_roa_pct`(float)=5.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `roa_filter`(roa filter)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `roa_filter` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_roa_pct=5.0`면 해당 지표가 최소 5.0 이상인 종목만 통과합니다. `min_roa_pct=5.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `roa_filter` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `roa_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `roce_filter` | `fundamental` | `min_roce_pct`(float)=10.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `roce_filter`(roce filter)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `roce_filter` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_roce_pct=10.0`면 해당 지표가 최소 10.0 이상인 종목만 통과합니다. `min_roce_pct=10.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `roce_filter` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `roce_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `roe` | `Fundamental` | `min_roe`(float)=15.0, `max_roe`(float)=None | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `roe`(roe)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `roe` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_roe=15.0`면 해당 지표가 최소 15.0 이상인 종목만 통과합니다. `min_roe=15.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_roe=None`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `roe` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `roe`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `roic` | `Fundamental` | `min_roic`(float)=15.0, `max_roic`(float)=None | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `roic`(roic)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `roic` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_roic=15.0`면 해당 지표가 최소 15.0 이상인 종목만 통과합니다. `min_roic=15.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다; `max_roic=None`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `roic` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `roic`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `rolling_sharpe_filter` | `risk` | `lookback_days`(int)=60, `min_sharpe`(float)=1.0 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `rolling_sharpe_filter`(rolling sharpe filter)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `rolling_sharpe_filter` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_sharpe=1.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `rolling_sharpe_filter` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `rolling_sharpe_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `rolling_sortino_filter` | `risk` | `lookback_days`(int)=60, `min_sortino`(float)=1.0 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `rolling_sortino_filter`(rolling sortino filter)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `rolling_sortino_filter` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_sortino=1.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `rolling_sortino_filter` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `rolling_sortino_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `rsi_overbought` | `rsi` | `threshold`(float)=70, `period`(int)=14 | "지금이 과열/과매도 구간이라 되돌림 확률이 높은가?" `rsi_overbought`(rsi overbought)은(는) 역추세/스윙 타이밍 전략에서 신호 선별에 쓰기 좋습니다. 특히 `rsi_overbought` 고유 규칙을 다른 osc 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `threshold=70`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `threshold=70`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `rsi_overbought` 기준입니다. | 과매수/과매도 신호만으로 역베팅하면 강한 추세에서 손실이 커질 수 있습니다. 추가로 `rsi_overbought`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_RSICORE` |
| `rsi_oversold` | `rsi` | `threshold`(float)=35, `period`(int)=14 | "지금이 과열/과매도 구간이라 되돌림 확률이 높은가?" `rsi_oversold`(rsi oversold)은(는) 역추세/스윙 타이밍 전략에서 신호 선별에 쓰기 좋습니다. 특히 `rsi_oversold` 고유 규칙을 다른 osc 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `threshold=35`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `threshold=35`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `rsi_oversold` 기준입니다. | 과매수/과매도 신호만으로 역베팅하면 강한 추세에서 손실이 커질 수 있습니다. 추가로 `rsi_oversold`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_RSICORE` |
| `rsi_range` | `rsi` | `lower`(float)=50, `upper`(float)=70, `period`(int)=14 | "지금이 과열/과매도 구간이라 되돌림 확률이 높은가?" `rsi_range`(rsi range)은(는) 역추세/스윙 타이밍 전략에서 신호 선별에 쓰기 좋습니다. 특히 `rsi_range` 고유 규칙을 다른 osc 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lower=50`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lower=50`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `upper=70`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `rsi_range` 기준입니다. | 과매수/과매도 신호만으로 역베팅하면 강한 추세에서 손실이 커질 수 있습니다. 추가로 `rsi_range`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_RSICORE` |
| `semivariance_filter` | `risk` | `lookback_days`(int)=60, `max_semivariance`(float)=0.0004 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `semivariance_filter`(semivariance filter)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `semivariance_filter` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_semivariance=0.0004`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `semivariance_filter` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `semivariance_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `shareholder_yield_filter` | `fundamental` | `min_yield_pct`(float)=2.0 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `shareholder_yield_filter`(shareholder yield filter)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `shareholder_yield_filter` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_yield_pct=2.0`면 해당 지표가 최소 2.0 이상인 종목만 통과합니다. `min_yield_pct=2.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `shareholder_yield_filter` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `shareholder_yield_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `short_float_pct_filter` | `sentiment` | `max_short_float_pct`(float)=15.0 | "특정 시점(실적·캘린더)에 반복되는 패턴이 있는가?" `short_float_pct_filter`(short float pct filter)은(는) 이벤트 드리븐/시즌 전략에서 신호 선별에 쓰기 좋습니다. 특히 `short_float_pct_filter` 고유 규칙을 다른 event 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `max_short_float_pct=15.0`면 해당 지표가 최대 15.0 이하인 종목만 통과합니다. `max_short_float_pct=15.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `short_float_pct_filter` 기준입니다. | 거래비용·슬리피지 반영 후 알파가 사라지기 쉬워 체결 가능성 검증이 필수입니다. 추가로 `short_float_pct_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_SHORT` |
| `short_interest_ratio_filter` | `sentiment` | `max_ratio`(float)=5.0 | "특정 시점(실적·캘린더)에 반복되는 패턴이 있는가?" `short_interest_ratio_filter`(short interest ratio filter)은(는) 이벤트 드리븐/시즌 전략에서 신호 선별에 쓰기 좋습니다. 특히 `short_interest_ratio_filter` 고유 규칙을 다른 event 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `max_ratio=5.0`면 해당 지표가 최대 5.0 이하인 종목만 통과합니다. `max_ratio=5.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `short_interest_ratio_filter` 기준입니다. | 거래비용·슬리피지 반영 후 알파가 사라지기 쉬워 체결 가능성 검증이 필수입니다. 추가로 `short_interest_ratio_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_SHORT` |
| `sma_slope` | `momentum` | `period`(int)=20, `lookback_days`(int)=5, `min_slope_pct`(float)=0.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `sma_slope`(sma slope)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `sma_slope` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `min_slope_pct=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `sma_slope` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `sma_slope`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `stochastic_cross_signal` | `oscillator` | `k_period`(int)=14, `d_period`(int)=3, `lookback_days`(int)=5 | "지금이 과열/과매도 구간이라 되돌림 확률이 높은가?" `stochastic_cross_signal`(stochastic cross signal)은(는) 역추세/스윙 타이밍 전략에서 신호 선별에 쓰기 좋습니다. 특히 `stochastic_cross_signal` 고유 규칙을 다른 osc 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `k_period=14`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `k_period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `d_period=3`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `stochastic_cross_signal` 기준입니다. | 과매수/과매도 신호만으로 역베팅하면 강한 추세에서 손실이 커질 수 있습니다. 추가로 `stochastic_cross_signal`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_STOCH` |
| `stochastic_divergence` | `accumulation` | `k_period`(int)=14, `d_period`(int)=3, `lookback`(int)=20, `divergence_threshold`(float)=5.0 | "지금이 과열/과매도 구간이라 되돌림 확률이 높은가?" `stochastic_divergence`(stochastic divergence)은(는) 역추세/스윙 타이밍 전략에서 신호 선별에 쓰기 좋습니다. 특히 `stochastic_divergence` 고유 규칙을 다른 osc 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `k_period=14`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `k_period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `d_period=3`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `divergence_threshold=5.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `stochastic_divergence` 기준입니다. | 과매수/과매도 신호만으로 역베팅하면 강한 추세에서 손실이 커질 수 있습니다. 추가로 `stochastic_divergence`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_STOCH` |
| `stochastic_level` | `accumulation` | `threshold`(float)=20.0, `condition`(str)=below, `k_period`(int)=14, `d_period`(int)=3 | "지금이 과열/과매도 구간이라 되돌림 확률이 높은가?" `stochastic_level`(stochastic level)은(는) 역추세/스윙 타이밍 전략에서 신호 선별에 쓰기 좋습니다. 특히 `stochastic_level` 고유 규칙을 다른 osc 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `threshold=20.0`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `threshold=20.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `condition=below`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `k_period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `d_period=3`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `stochastic_level` 기준입니다. | 과매수/과매도 신호만으로 역베팅하면 강한 추세에서 손실이 커질 수 있습니다. 추가로 `stochastic_level`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_STOCH` |
| `stochrsi_signal` | `oscillator` | `rsi_period`(int)=14, `stoch_period`(int)=14, `mode`(str)=oversold | "이 조건이 전략 전체의 품질을 올려주는가?" `stochrsi_signal`(stochrsi signal)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `stochrsi_signal` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `rsi_period=14`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `rsi_period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `stoch_period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `mode=oversold`는 전략 방향과 맞춰야 해석이 일관됩니다. 이 해석은 `stochrsi_signal` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `stochrsi_signal`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `support_retest_signal` | `price_action` | `support_lookback`(int)=40, `tolerance_pct`(float)=1.0 | "이 조건이 전략 전체의 품질을 올려주는가?" `support_retest_signal`(support retest signal)은(는) 멀티팩터 보조 전략에서 신호 선별에 쓰기 좋습니다. 특히 `support_retest_signal` 고유 규칙을 다른 general 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `support_lookback=40`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `support_lookback=40`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `tolerance_pct=1.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다. 이 해석은 `support_retest_signal` 기준입니다. | 단일 조건 과신을 피하고 최소 2~3개 보완 조건을 함께 써야 과최적화 위험이 줄어듭니다. 추가로 `support_retest_signal`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `trix_cross` | `momentum` | `period`(int)=15, `signal_period`(int)=9, `lookback_days`(int)=5, `direction`(str)=bullish | "지금 추세가 실제로 이어질 가능성이 높은가?" `trix_cross`(trix cross)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `trix_cross` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=15`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=15`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `signal_period=9`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `direction=bullish`는 전략 방향과 맞춰야 해석이 일관됩니다. 이 해석은 `trix_cross` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `trix_cross`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `turn_of_month_effect` | `time` | `window_days`(int)=3, `min_avg_return_pct`(float)=0.0 | "특정 시점(실적·캘린더)에 반복되는 패턴이 있는가?" `turn_of_month_effect`(turn of month effect)은(는) 이벤트 드리븐/시즌 전략에서 신호 선별에 쓰기 좋습니다. 특히 `turn_of_month_effect` 고유 규칙을 다른 event 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `window_days=3`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `window_days=3`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `min_avg_return_pct=0.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `turn_of_month_effect` 기준입니다. | 거래비용·슬리피지 반영 후 알파가 사라지기 쉬워 체결 가능성 검증이 필수입니다. 추가로 `turn_of_month_effect`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_SEASONAL` |
| `turnover_ratio_min` | `volume` | `min_turnover_ratio`(float)=0.01 | "이 회사의 내재가치와 재무체력이 가격 대비 충분한가?" `turnover_ratio_min`(turnover ratio min)은(는) 가치투자/퀄리티 전략에서 신호 선별에 쓰기 좋습니다. 특히 `turnover_ratio_min` 고유 규칙을 다른 fund 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_turnover_ratio=0.01`면 해당 지표가 최소 0.01 이상인 종목만 통과합니다. `min_turnover_ratio=0.01`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `turnover_ratio_min` 기준입니다. | 업종 구조 차이(금융/유틸리티/성장주)로 절대값 비교가 왜곡될 수 있어 동일 업종 내 비교가 안전합니다. 추가로 `turnover_ratio_min`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VAL` |
| `ulcer_index_filter` | `risk` | `lookback_days`(int)=60, `max_ulcer_index`(float)=10.0 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `ulcer_index_filter`(ulcer index filter)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ulcer_index_filter` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_ulcer_index=10.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `ulcer_index_filter` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `ulcer_index_filter`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `ultimate_oscillator_signal` | `oscillator` | `min_value`(float)=50.0 | "지금이 과열/과매도 구간이라 되돌림 확률이 높은가?" `ultimate_oscillator_signal`(ultimate oscillator signal)은(는) 역추세/스윙 타이밍 전략에서 신호 선별에 쓰기 좋습니다. 특히 `ultimate_oscillator_signal` 고유 규칙을 다른 osc 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `min_value=50.0`면 해당 지표가 최소 50.0 이상인 종목만 통과합니다. `min_value=50.0`로 올리면 통과 종목이 줄고 신호 강도는 높아집니다. 이 해석은 `ultimate_oscillator_signal` 기준입니다. | 과매수/과매도 신호만으로 역베팅하면 강한 추세에서 손실이 커질 수 있습니다. 추가로 `ultimate_oscillator_signal`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `volatility_n_day` | `risk` | `lookback_days`(int)=60, `max_annualized_vol_pct`(float)=25.0 | "수익보다 먼저 손실 변동을 감당할 수 있는 종목인가?" `volatility_n_day`(volatility n day)은(는) 리스크 관리/포지션 사이징 전략에서 신호 선별에 쓰기 좋습니다. 특히 `volatility_n_day` 고유 규칙을 다른 risk 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `lookback_days=60`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `lookback_days=60`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_annualized_vol_pct=25.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `volatility_n_day` 기준입니다. | 과거 위험이 미래 위험을 완전히 설명하지 못하므로 급격한 레짐 전환 구간을 별도 점검해야 합니다. 추가로 `volatility_n_day`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `volume_above_avg` | `volume` | `multiplier`(float)=1.0, `period`(int)=20 | "가격 뒤에서 자금이 실제로 유입되고 있는가?" `volume_above_avg`(volume above avg)은(는) 매집·수급 확인 전략에서 신호 선별에 쓰기 좋습니다. 특히 `volume_above_avg` 고유 규칙을 다른 flow 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `multiplier=1.0`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `multiplier=1.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `volume_above_avg` 기준입니다. | 저유동성 종목은 체결 왜곡이 커서 지표 신호가 실전 수익으로 연결되지 않을 수 있습니다. 추가로 `volume_above_avg`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `volume_below_avg` | `accumulation` | `multiplier`(float)=1.0, `period`(int)=20 | "가격 뒤에서 자금이 실제로 유입되고 있는가?" `volume_below_avg`(volume below avg)은(는) 매집·수급 확인 전략에서 신호 선별에 쓰기 좋습니다. 특히 `volume_below_avg` 고유 규칙을 다른 flow 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `multiplier=1.0`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `multiplier=1.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `volume_below_avg` 기준입니다. | 저유동성 종목은 체결 왜곡이 커서 지표 신호가 실전 수익으로 연결되지 않을 수 있습니다. 추가로 `volume_below_avg`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `volume_spike` | `volume` | `multiplier`(float)=1.5, `period`(int)=20 | "가격 뒤에서 자금이 실제로 유입되고 있는가?" `volume_spike`(volume spike)은(는) 매집·수급 확인 전략에서 신호 선별에 쓰기 좋습니다. 특히 `volume_spike` 고유 규칙을 다른 flow 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `multiplier=1.5`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `multiplier=1.5`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `volume_spike` 기준입니다. | 저유동성 종목은 체결 왜곡이 커서 지표 신호가 실전 수익으로 연결되지 않을 수 있습니다. 추가로 `volume_spike`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `vpci_divergence` | `accumulation` | `price_max_range_pct`(float)=5.0, `short_period`(int)=5, `long_period`(int)=20, `lookback`(int)=20 | "가격 뒤에서 자금이 실제로 유입되고 있는가?" `vpci_divergence`(vpci divergence)은(는) 매집·수급 확인 전략에서 신호 선별에 쓰기 좋습니다. 특히 `vpci_divergence` 고유 규칙을 다른 flow 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `price_max_range_pct=5.0`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `price_max_range_pct=5.0`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `short_period=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `long_period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `vpci_divergence` 기준입니다. | 저유동성 종목은 체결 왜곡이 커서 지표 신호가 실전 수익으로 연결되지 않을 수 있습니다. 추가로 `vpci_divergence`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `vpci_trend` | `accumulation` | `direction`(str)=up, `short_period`(int)=5, `long_period`(int)=20, `lookback`(int)=10 | "지금 추세가 실제로 이어질 가능성이 높은가?" `vpci_trend`(vpci trend)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `vpci_trend` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `direction=up`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `direction=up`는 전략 방향과 맞춰야 해석이 일관됩니다; `short_period=5`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `long_period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback=10`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `vpci_trend` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `vpci_trend`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_FACTORS` |
| `vwap_cross_signal` | `volume` | `period`(int)=20, `lookback_days`(int)=3 | "지금 추세가 실제로 이어질 가능성이 높은가?" `vwap_cross_signal`(vwap cross signal)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `vwap_cross_signal` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `lookback_days=3`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다. 이 해석은 `vwap_cross_signal` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `vwap_cross_signal`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VWAP` |
| `vwap_distance_pct` | `volume` | `period`(int)=20, `max_distance_pct`(float)=5.0 | "지금 추세가 실제로 이어질 가능성이 높은가?" `vwap_distance_pct`(vwap distance pct)은(는) 추세추종/모멘텀 전략에서 신호 선별에 쓰기 좋습니다. 특히 `vwap_distance_pct` 고유 규칙을 다른 trend 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=20`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=20`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `max_distance_pct=5.0`를 낮추면 보수적으로 걸러지고 과열 종목 유입이 줄어듭니다. 이 해석은 `vwap_distance_pct` 기준입니다. | 횡보장에서는 휩쏘가 급증하므로 거래량 또는 변동성 필터를 함께 붙이는 편이 좋습니다. 추가로 `vwap_distance_pct`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_VWAP` |
| `williams_r_reversal` | `momentum` | `period`(int)=14, `oversold`(float)=-80, `overbought`(float)=-20, `direction`(str)=bullish | "지금이 과열/과매도 구간이라 되돌림 확률이 높은가?" `williams_r_reversal`(williams r reversal)은(는) 역추세/스윙 타이밍 전략에서 신호 선별에 쓰기 좋습니다. 특히 `williams_r_reversal` 고유 규칙을 다른 osc 계열 조건과 교차검증할 때 효과가 큽니다. | 예: `period=14`를 기준점으로 백테스트 구간별 성능 차이를 비교하세요. `period=14`는 관측 구간으로, 짧으면 민감하고 길면 안정적입니다; `oversold=-80`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `overbought=-20`를 기준값으로 시작해 민감도 테스트(±20%)를 권장합니다; `direction=bullish`는 전략 방향과 맞춰야 해석이 일관됩니다. 이 해석은 `williams_r_reversal` 기준입니다. | 과매수/과매도 신호만으로 역베팅하면 강한 추세에서 손실이 커질 수 있습니다. 추가로 `williams_r_reversal`는 단독 판단보다 손절·분할진입 규칙과 함께 써야 실전 재현성이 높습니다. | `S_WILLR` |

## 실전 적용 순서(권장)
- 1) 유동성 필터 고정: `min_volume`, `avg_trading_value`, `turnover_ratio_min`
- 2) 전략 축 선택: 추세/역추세/이벤트 중 1~2개만 우선 적용
- 3) 리스크 제한: `volatility`, `drawdown`, `ulcer`, `beta` 계열로 tail risk 제어
- 4) fundamental/quality 조건은 마지막에 얇게 추가 (과필터링 방지)
- 5) 튜닝은 default 기준 ±20~30% 3점 비교 후 아웃샘플 검증
